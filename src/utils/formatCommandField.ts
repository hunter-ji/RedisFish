export const FormatCommandField = (field: string): string => {
  // if (field.indexOf(' ') >= 0) {
  //   if (field.indexOf('\'') >= 0) return `"${field}"`
  //   return `'${field}'`
  // }

  // if (field.includes(' ')) {
  //   return `'${field}'`
  // }

  return field
}
